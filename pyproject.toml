[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "pymotility"
version = "0.1"
dependencies = ["numpy<1.23", "scipy", "matplotlib", "scikit-image", "scikit-video", "opencv-python", "pytest"]
